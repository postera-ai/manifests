apiVersion: v1
kind: ConfigMap
metadata:
  name: config-defaults
  namespace: knative-serving
data:
  # Cap on per-revision timeoutSeconds; Lab on Knative needs up to ~1250s.
  # Paired with patches/activator-grace-period.yaml.
  max-revision-timeout-seconds: "1800"
