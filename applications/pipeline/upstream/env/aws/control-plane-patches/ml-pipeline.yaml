apiVersion: apps/v1
kind: Deployment
metadata:
  name: ml-pipeline
spec:
  template:
    spec:
      # Already Burstable (250m/500Mi); just raise its scheduling/eviction priority.
      priorityClassName: kubeflow-control-plane
